/**
 * Client wrapper for pipeline execution
 * Manages state and displays results
 */

import { RunPipelineButton } from "./RunPipelineButton";

export function RunPipelineClient() {
  return <RunPipelineButton />;
}
